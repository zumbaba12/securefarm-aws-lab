{
  "name": "securefarm-lab",
  "version": "0.1.0",
  "private": true,
  "description": "SecureFarm AWS Lab - deliberately vulnerable farmer monitor app for security practice",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "seed": "npm run seed --workspace server",
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:api": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "test": "npm test --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
