# App Workspace

Claude should create the SecureFarm application in this directory.

The first implementation should be minimal and runnable locally. See [../handoff/claude-prompt.md](../handoff/claude-prompt.md) for the active build prompt.
